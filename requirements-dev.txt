# Training-only dependencies — NOT installed on Render
# Install locally with:  pip install -r requirements-dev.txt
-r requirements.txt

pandas>=2.0.0,<3.0.0
scikit-learn>=1.2.0,<2.0.0
torchvision==0.19.1+cpu
